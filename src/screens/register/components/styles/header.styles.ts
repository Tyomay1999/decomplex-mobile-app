import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: { alignItems: "center", marginBottom: 40 },
  h1: { fontSize: 32, fontWeight: "700", letterSpacing: -0.5 },
  sub: { marginTop: 8, fontSize: 16, opacity: 0.9 },
});
