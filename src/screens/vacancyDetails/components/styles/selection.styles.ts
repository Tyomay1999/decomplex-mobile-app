import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    gap: 14,
  },
  card: {
    borderRadius: 16,
    padding: 16,
    borderWidth: 1,
  },
  line: {
    borderRadius: 8,
  },
  tagsRow: {
    flexDirection: "row",
    gap: 8,
    paddingHorizontal: 4,
  },
});
