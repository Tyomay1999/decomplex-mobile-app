import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  actionsWrap: { paddingHorizontal: 16, paddingTop: 24 },
  lockWrap: { alignItems: "center", paddingVertical: 34 },

  primaryBtn: {
    height: 52,
    borderRadius: 16,
    alignItems: "center",
    justifyContent: "center",
    marginBottom: 12,
  },
  secondaryBtn: {
    height: 52,
    borderRadius: 16,
    alignItems: "center",
    justifyContent: "center",
    borderWidth: 1,
  },
});
